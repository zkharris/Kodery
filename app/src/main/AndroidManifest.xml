<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zacharyharris.kodery">

    <application
        android:allowBackup="true"
        android:icon="@drawable/kodery_icon"
        android:label="@string/app_name"
        android:logo="@drawable/kodery_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.MainActivity"
            android:configChanges="orientation|screenSize">
        </activity>
        <activity android:name=".UI.SignInActivity" />
        <activity android:name=".UI.UsersActivity" />
        <activity android:name=".UI.InvitesActivity" />
        <activity android:name=".UI.SingleBoardActivity"
            android:configChanges="orientation|screenSize">
        </activity>
        <activity android:name=".UI.SingleListActivity"
            android:configChanges="orientation|screenSize">
        </activity>
        <activity android:name=".UI.BoardMembersActivity" />
        <activity android:name=".UI.SingleTaskActivity"
            android:configChanges="orientation|screenSize">
        </activity>
        <activity android:name=".UI.ChatActivity"
            android:configChanges="orientation|screenSize">
        </activity>
        <activity android:name=".UI.MoveTaskActivity" />
        <activity android:name=".UI.LoadActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>